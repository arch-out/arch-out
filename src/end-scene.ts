export default class EndScene extends Phaser.Scene {
    public static KEY: string = "EndScene";

    constructor() {
        super({
            key: EndScene.KEY,
            active: true,
            visible: true
        });
    }

    update() {

    }

    create() {
    }
}